export interface OrderDataType {
  customer: string;
  destination: string;
  event_name: string;
  id: string;
  item: string;
  price: number;
  sent_at_second: number;
}

export interface OrderData